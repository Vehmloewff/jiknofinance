import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
	appId: 'com.jikno.finance',
	appName: 'Jikno Finance',
	webDir: 'public',
	bundledWebRuntime: false,
}

export default config
