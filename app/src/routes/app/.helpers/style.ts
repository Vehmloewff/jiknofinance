export function makeBackground(color: string) {
	return 'linear-gradient(var(--background1), var(--background))'
}

export function makeCoverBackground(color: string) {
	return `var(--background1)`
}
